<%= form_with(model: education_point) do |form| %>
  <% if education_point.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(education_point.errors.count, "error") %> prohibited this education_point from being saved:</h2>

      <ul>
        <% education_point.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :text, style: "display: block" %>
    <%= form.text_field :text %>
  </div>

  <div>
    <%= form.label :education_id, style: "display: block" %>
    <%= form.text_field :education_id %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
